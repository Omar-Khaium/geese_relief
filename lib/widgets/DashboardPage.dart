import 'package:flutter/material.dart';

class DashboardPageWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("Dhasbord Content",style: new TextStyle(fontSize: 20),)),
    );

  }
}
