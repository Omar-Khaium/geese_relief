class CustomerHistory {
  int id;
  String firstName;
  String lastName;
  String checkedIn;
  String checkedOut;
  int geeseCount;

  CustomerHistory(this.id, this.firstName, this.lastName, this.checkedIn,
      this.checkedOut, this.geeseCount);
}